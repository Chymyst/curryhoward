package io.chymyst.ch

object MonadEither {
//  implicit class EitherWithFlatmap[A, B](e: Either[A, B]) {
//    def flatMap[C](f: B ⇒ Either[A, C]): Either[A, C] = e match {
//      case Left(a) ⇒ Left(a)
//      case Right(b) ⇒ f(b)
//    }
//  }
}
